build/
build-*/
.cache/
*.user
.qtc_clangd/
reference/
CMakeFiles/
.worktrees/
.claude/worktrees/
.superpowers/
.DS_Store
**/.DS_Store
.obsidian/
# Bench log written by `./run.sh 2>&1 | tee nereus-run.log` during the
# 2026-08-11 monitor debugging workflow — session-local, never committed.
nereus-run.log
# Same workflow, ctest output: `ctest … | tee test-failures.log`.
test-failures.log
# CTest temp output — auto-regenerated by every local ctest invocation;
# review-flagged on PR #279 (#6) for ending up checked in by accident.
Testing/Temporary/
Testing/CTestTestfile.cmake

# DeepFilterNet3 pre-built libs (built by setup-deepfilter.sh, not checked in)
third_party/deepfilter/lib/
third_party/deepfilter/models/*.tar.gz
.aether-sha
build-tests/

# Persoenliche Theme-Dateien. Design gehoert dem Betreiber, nicht dem Repo.
# Siehe docs/design/ROADMAP.md.
themes/
