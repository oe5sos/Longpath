# Stand am 2026-08-15 — Übergabe

Für die nächste Sitzung. Alles hier ist geprüft, nicht vermutet.

## Zustand

**668/668 Tests grün, gebaut, alles committet.** Arbeitsbaum sauber bis
`bb14acb8`.

```
cd ~/Desktop/neureus/NereusSDR && ./tools/run_tests.sh
```

Der frühere Eintrag hier nannte 667 Tests und rund 130 ungesicherte
Dateien seit `004ecca6`. Beides ist erledigt: der Commit `66ed8a7e` hat
den Rückstand aufgeräumt, und die Arbeit des Nachmittags liegt in vier
weiteren Commits.

## Was heute entstanden ist

**Kommandoleiste** (`widgets/CommandBar`) — MODE und STEP als Pillen über
die volle Breite, hängt an der aktiven Empfangskette. BAND/FILTER/FAVORITES
fehlen absichtlich: welche drei vorne stehen, entscheidet der Betreiber.

**Das Plus** (`widgets/WidgetPicker`) — Kategoriespalte, Suche, Karten.
`Qt::Tool`, nicht `Qt::Popup`: verschiebbar, bleibt offen. Höhe auf 560
gedeckelt. Im Auswähler stehen auch Knopfleiste am Spektrum und
Statuszeile (`kChromeOverlayId`, `kChromeStatusId` in MainWindow).

**Verschieben** (`AppletPanelWidget`) — die Griffpunkte ⋮⋮ ziehen jetzt,
Nachbarn weichen live aus. Reihenfolge in `AppletStackOrder` UND im Profil;
der Schlüssel ist der Startwert für das erste Profil, kein Rest.

**Profile** (`LayoutProfiles`, `widgets/ProfileRail`) — Abzeichen links,
Rechtsklick für Umbenennen/Duplizieren/Löschen. `create()` nimmt eine
Aufnahme, `createWith()` bekommt einen Zustand vorgegeben — das Plus legt
LEER an. Kein automatisches Umschalten (Entscheidung des Betreibers).

**Farbe** — Wasserfall, S-Meter, Spektrumkurve, Gitter, SWR-Kurve, Karte
und EQ-Chrome laufen über Theme-Rollen. Die Wasserfall-Historie hält
Intensität statt Farbe (`WaterfallHistoryBuffer`, portiert aus AetherSDR,
GPLv3), womit ein Schemawechsel auch alte Zeilen erreicht.

**FFT 4096 → 16384** — 47 Hz je Bin waren bei 192 kHz weniger Messwerte,
als das Fenster Pixel hat. Der Detektor interpolierte statt auszuwählen.
`kDefaultFftSize` steht jetzt an einer Stelle.

**Migrationen v7 und v8** ziehen gespeicherte Werte nach, aber nur, wenn
noch der alte Vorgabewert steht.

## Was am Nachmittag dazukam

**Der dritte Zustand im Antennenfenster** (`8dcddad9`) — während eines
laufenden Sweeps nannte der Kopf das neue Band, während Kurve, Kacheln,
Spanne und Tabelle darunter weiter das vorige beschrieben. Kein
vergessener Aufruf: die Auswertung hatte einen einzigen Eingang, und der
hing an `analysisReady`, also am *Ende* eines Sweeps. Neu ist die
fehlende Kante `sweepStartedFor(band, lo, hi)`, ausgelöst beim Tasten.

Vier Regeln, von denen die dritte aus den anderen herausfällt statt
eigens dazustehen: anderes Band dämpft · gleiches Band nicht · ein Lauf
ohne gültige Punkte bleibt dauerhaft gedämpft, weil nur eine echte
Messung den Zustand löscht · eine echte Messung löst ihn. Die Kurve
bleibt sichtbar, verliert Deckkraft und trägt rechts oben `20 m ·
ÜBERHOLT`.

Die harte Grenze ist dabei eine Funktion geworden:
`SwrCurveWidget::fadesWhenSuperseded(rolle)` entscheidet allein, welche
Farbe zurücktreten darf, und `paintEvent` schickt **jede** Farbe
hindurch — auch `danger` und `measured-border`, die unverändert
zurückkommen. Wer das SWR-Rot künftig mitdämpfen will, muss diese eine
Funktion ändern, und dann fällt `theWarningColoursNeverFade` um.

**Spektrum neutral** (`bb14acb8`) — die Naht quer durchs Fenster ist
weg. Das Gitter war schon in Ordnung (läuft über `grid` / `grid-text`);
blau waren nur die Flächen, neun Stellen hart im Malcode. Der
Spot-Aufklapper trug die alte Palette vollständig und läuft jetzt auf
`app-bg` / `border` / `text` / `sel-bg` / `sel-text`. **`#00b4d8` kommt
in `SpectrumWidget.cpp` nicht mehr vor.**

Neue Rolle **`sel-text`** (`#cfe2f5`) — den Auswahltext gab es in der
Rollentabelle noch nicht. `oe5sos.example.json` hat jetzt 71 Rollen.

**`syntax_check.sh` findet Qt selbst** (`8709eb2c`) — Reihenfolge
`NEREUS_QTINC` → `qmake -query` → flacher Baum → macOS-Frameworks, für
die es sich den flachen Baum als Symlinks unter `.cache/qtinc` baut.
Kein Setzen einer Umgebungsvariablen mehr nötig.

## Drei Fehler, die beim Prüfen herausfielen

Keiner davon war gesucht; alle drei fielen an, weil ein Werkzeug zum
ersten Mal auf dieser Maschine lief.

1. **`--all-changed` hat auf macOS nie funktioniert.** `mapfile` ist ein
   Bash-4-Builtin, macOS liefert 3.2. Der Lauf brach ab und meldete dann
   das *Wort* `--all-changed` als fehlende Datei — ein Fehler,
   Rückgabewert ungleich null, und der ganze geänderte Satz ungeprüft.
2. **Der CPU-Bau übersetzte nicht.** `setShowPerfOverlay()` und
   `showPerfOverlay()` standen außerhalb des
   `NEREUS_GPU_SPECTRUM`-Blocks, ihre Member darin. `CMakeLists.txt:420`
   macht den GPU-Pfad zur Option, und ab Zeile 434 schaltet CMake ihn
   **von selbst ab**, wenn Qt älter als 6.7 ist oder ShaderTools bzw.
   GuiPrivate fehlen. Auf diesem Mac fehlen die privaten Qt-Header
   ohnehin — ein Homebrew-Update hätte gereicht.
3. **`#include <QThread>` fehlte** in `SpectrumWidget.cpp`. Getragen hat
   das allein der vorkompilierte Header; mit `-DNEREUS_USE_PCH=OFF` —
   laut `CMakeLists.txt:1590` die Einstellung für genau eine
   Header-Hygiene-Prüfung — fiel die Datei um.

## Was hier nicht geprüft werden kann

Homebrew liefert Qts private `rhi`-Header nicht (kein `qrhi.h` unter
`/opt/homebrew`). Damit lässt sich der **GPU-Pfad auf dieser Maschine
nicht übersetzen** — nur der CPU-Pfad, den `syntax_check.sh` benutzt.
Der GPU-Bau wird allein durch `./build.sh` abgedeckt, und das ist die
Standardkonfiguration; ein Fehler dort fällt beim ersten Bauen auf.

Praktische Folge für den Prüflauf: `#ifdef`-Fehler in
`SpectrumWidget.cpp` können hier nur den CPU-Zweig treffen, weil bei
gesetztem Makro alles zwischen `#ifdef` und `#endif` erhalten bleibt.

## Zwei Korrekturen an der Aufgabenliste

**#88 war schon erledigt**, ohne dass jemand daran gearbeitet hat: der
`ThemeFilter` hängt an `Polish` UND `StyleChange`, und jedes
`setStyleSheet` löst StyleChange aus. Die echte Lücke sind `QPainter`-
Farben, und die stehen in #96/#101.

**`tst_swr_sweep_controller`** fiel sporadisch durch. Kein Fehler im
Controller: elf Pumpschleifen mit 3000-ms-Zeitlimit bei 2,4–3,1 s
Laufzeit. Jetzt eine Zahl, 30 s, und der Abbruch sagt, dass er einer ist.

## Was als Nächstes ansteht

**#103 Farbordnung** — grösste sichtbare Wirkung, kleinster Eingriff.
Befund: die gelben und grünen Rufzeichen sind NICHT unsere Gestaltung.
`SpectrumWidget.cpp:6058` — Override → DXCC → Spot-Farbe → Cyan. Der
Schalter `m_spotOverrideColors` existiert bereits. Vorschlag: Override
zur Vorgabe mit gedämpftem Ton, DXCC-Einfärbung als bewusste Wahl
behalten. Danach: Grau = Rahmen, Stahlblau = ausgewählt, Creme-Bernstein
= Messwert, Rot = NUR Grenzen, ein Signalton für Spots.

**#102 Wasserfall oben** — der Betreiber wollte hinsehen. Rampe steht auf
0,00 / 0,30 / 0,48 / 0,64 / 0,78 / 0,92 / 1,00. Zwei Fehlversuche liegen
hinter uns (0,16 = Nebel, 0,45 = Loch), beide sind jetzt durch
`theDarkEndCoversRoughlyTheLowerThird` abgesichert.

**#99 Container ins Profil** — braucht `captureState()`/`applyState()` im
`ContainerManager` plus Abräumen vor dem Herstellen; `restoreState()`
legt heute NEUE Container an, ein Profilwechsel würde sie verdoppeln.

**#101 Meter** — 88 rohe Farben, 44 in `ItemGroup.cpp`. Bei Metern IST
Farbe Bedeutung. Kein Skript.

**#100 Schritt 3, Frequenzrahmen je Zeile** — der grösste Brocken. Siehe
`WASSERFALL-AETHER.md`. Heute wird die Historie beim Schwenken verworfen:
lieber leer als falsch.

## Zwei Dinge, die stehen bleiben müssen

Die **Bandkante bleibt rot** und das **SWR-Rot bleibt kräftig**. Beides
sind Sicherheitsmarken. Eine Warnfarbe leiser zu drehen, weil sie
auffällt, schafft die Warnung ab.

Nichts in der Oberfläche darf zum Senden ausserhalb der Amateurbänder
einladen, auch nicht bei 1 W. `BandPlanGuard` bleibt die Instanz.

Seit `8dcddad9` steht der erste Satz nicht mehr nur hier. Die
Palettenumstellung im Spektrum hat `m_bandEdgeColor{255, 0, 0}`
ausgelassen, und in der SWR-Kurve entscheidet
`SwrCurveWidget::fadesWhenSuperseded()` als einzige Stelle, welche Farbe
zurücktreten darf — `theWarningColoursNeverFade` fällt um, sobald jemand
`danger` oder `measured-border` dazunimmt. Das ist der Unterschied
zwischen einer Regel in einem Dokument und einer, die gilt.

Die Dämpfung der überholten Auswertung war der erste Fall, in dem
„gedämpft" und „SWR-Rot bleibt kräftig" direkt kollidierten. Ein
pauschales `setOpacity` über den ganzen Malvorgang wäre der naheliegende
Weg gewesen und hätte die Grenzlinie mit abgedunkelt. Beim nächsten Mal
wird es wieder so aussehen, als wäre es nur eine Zeile.
