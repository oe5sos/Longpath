# Stand am 2026-08-15 — Übergabe

Für die nächste Sitzung. Alles hier ist geprüft, nicht vermutet.

## Zustand

667/667 Tests grün. **Rund 130 Dateien ungesichert** seit `004ecca6`.
Erster Schritt der nächsten Sitzung: Testlauf, dann Commit.

```
cd ~/Desktop/neureus/NereusSDR && ./tools/run_tests.sh
```

## Was heute entstanden ist

**Kommandoleiste** (`widgets/CommandBar`) — MODE und STEP als Pillen über
die volle Breite, hängt an der aktiven Empfangskette. BAND/FILTER/FAVORITES
fehlen absichtlich: welche drei vorne stehen, entscheidet der Betreiber.

**Das Plus** (`widgets/WidgetPicker`) — Kategoriespalte, Suche, Karten.
`Qt::Tool`, nicht `Qt::Popup`: verschiebbar, bleibt offen. Höhe auf 560
gedeckelt. Im Auswähler stehen auch Knopfleiste am Spektrum und
Statuszeile (`kChromeOverlayId`, `kChromeStatusId` in MainWindow).

**Verschieben** (`AppletPanelWidget`) — die Griffpunkte ⋮⋮ ziehen jetzt,
Nachbarn weichen live aus. Reihenfolge in `AppletStackOrder` UND im Profil;
der Schlüssel ist der Startwert für das erste Profil, kein Rest.

**Profile** (`LayoutProfiles`, `widgets/ProfileRail`) — Abzeichen links,
Rechtsklick für Umbenennen/Duplizieren/Löschen. `create()` nimmt eine
Aufnahme, `createWith()` bekommt einen Zustand vorgegeben — das Plus legt
LEER an. Kein automatisches Umschalten (Entscheidung des Betreibers).

**Farbe** — Wasserfall, S-Meter, Spektrumkurve, Gitter, SWR-Kurve, Karte
und EQ-Chrome laufen über Theme-Rollen. Die Wasserfall-Historie hält
Intensität statt Farbe (`WaterfallHistoryBuffer`, portiert aus AetherSDR,
GPLv3), womit ein Schemawechsel auch alte Zeilen erreicht.

**FFT 4096 → 16384** — 47 Hz je Bin waren bei 192 kHz weniger Messwerte,
als das Fenster Pixel hat. Der Detektor interpolierte statt auszuwählen.
`kDefaultFftSize` steht jetzt an einer Stelle.

**Migrationen v7 und v8** ziehen gespeicherte Werte nach, aber nur, wenn
noch der alte Vorgabewert steht.

## Zwei Korrekturen an der Aufgabenliste

**#88 war schon erledigt**, ohne dass jemand daran gearbeitet hat: der
`ThemeFilter` hängt an `Polish` UND `StyleChange`, und jedes
`setStyleSheet` löst StyleChange aus. Die echte Lücke sind `QPainter`-
Farben, und die stehen in #96/#101.

**`tst_swr_sweep_controller`** fiel sporadisch durch. Kein Fehler im
Controller: elf Pumpschleifen mit 3000-ms-Zeitlimit bei 2,4–3,1 s
Laufzeit. Jetzt eine Zahl, 30 s, und der Abbruch sagt, dass er einer ist.

## Was als Nächstes ansteht

**#103 Farbordnung** — grösste sichtbare Wirkung, kleinster Eingriff.
Befund: die gelben und grünen Rufzeichen sind NICHT unsere Gestaltung.
`SpectrumWidget.cpp:6058` — Override → DXCC → Spot-Farbe → Cyan. Der
Schalter `m_spotOverrideColors` existiert bereits. Vorschlag: Override
zur Vorgabe mit gedämpftem Ton, DXCC-Einfärbung als bewusste Wahl
behalten. Danach: Grau = Rahmen, Stahlblau = ausgewählt, Creme-Bernstein
= Messwert, Rot = NUR Grenzen, ein Signalton für Spots.

**#102 Wasserfall oben** — der Betreiber wollte hinsehen. Rampe steht auf
0,00 / 0,30 / 0,48 / 0,64 / 0,78 / 0,92 / 1,00. Zwei Fehlversuche liegen
hinter uns (0,16 = Nebel, 0,45 = Loch), beide sind jetzt durch
`theDarkEndCoversRoughlyTheLowerThird` abgesichert.

**#99 Container ins Profil** — braucht `captureState()`/`applyState()` im
`ContainerManager` plus Abräumen vor dem Herstellen; `restoreState()`
legt heute NEUE Container an, ein Profilwechsel würde sie verdoppeln.

**#101 Meter** — 88 rohe Farben, 44 in `ItemGroup.cpp`. Bei Metern IST
Farbe Bedeutung. Kein Skript.

**#100 Schritt 3, Frequenzrahmen je Zeile** — der grösste Brocken. Siehe
`WASSERFALL-AETHER.md`. Heute wird die Historie beim Schwenken verworfen:
lieber leer als falsch.

## Zwei Dinge, die stehen bleiben müssen

Die **Bandkante bleibt rot** und das **SWR-Rot bleibt kräftig**. Beides
sind Sicherheitsmarken. Eine Warnfarbe leiser zu drehen, weil sie
auffällt, schafft die Warnung ab.

Nichts in der Oberfläche darf zum Senden ausserhalb der Amateurbänder
einladen, auch nicht bei 1 W. `BandPlanGuard` bleibt die Instanz.
